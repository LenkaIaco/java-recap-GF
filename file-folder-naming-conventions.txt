1. java class / test class: pascal naming conv.:
	 CustomerService.java / CustomerServiceTest.java
2. other files: lowercase, hyphen/underscore instead of space 
	project-overview.md
3. folders (incl.java packages): lowercase, hyphens/underscore. Java package names follows reverse domain name parts in corporate environments:
	com.example.app		user-data/

Stick to:
a–z, 0–9, -, _, .

Avoid:
! @ # $ % ^ & * ( ) { } [ ] ; : ' " < > ? | \ /